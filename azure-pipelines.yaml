jobs:
- job: macOS
  pool:
    vmImage: 'macOS-10.14'
  steps:
    - script: 'brew install llvm'
      displayName: Brew install LLVM
    - script: "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs -o rustup.sh && sh rustup.sh -y"
      displayName: Rustup
    - script: |
        PATH=/usr/local/bin:$PATH             # Local bin (brew)
        PATH=~/.cargo/bin:$PATH               # Cargo installed binaries
        export PATH
        export LLVM_SYS_80_PREFIX=/usr/local/opt/llvm
        pip3 install virtualenv
        ./build.sh test
      displayName: './build.sh test'
- job: Linux
  pool:
    vmImage: 'ubuntu-16.04'
  container: 'ubuntu:bionic'
  steps:
    - script: |
        lscpu
        free -h
      displayName: Dump Box Configuration
    - script: |
        cat > llvm.list <<EOF
        # 8
        deb http://apt.llvm.org/xenial/ llvm-toolchain-xenial-8 main
        deb-src http://apt.llvm.org/xenial/ llvm-toolchain-xenial-8 main
        EOF
        sudo cp llvm.list /etc/apt/sources.list.d/
        sudo apt-get update
        sudo apt-get install -y llvm-8-dev python3 python3-pip python3-virtualenv zlib1g-dev libterminfo-dev
      displayName: Install LLVM
    - script: |
        sudo apt-get install -y gcc binutils
        gcc --version
        ld --version
      displayName: Install latest gcc
    # - script: 'pip3 install virtualenv'
    #   displayName: Virtualenv Install
    - script: "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs -o rustup.sh && sh rustup.sh -y"
      displayName: Rustup
    - script: |
        source ~/.cargo/env
        export LLVM_SYS_80_PREFIX=/usr/lib/llvm-8
        ./build.sh test
      displayName: './build.sh test'
